---
import { Image } from "astro:assets";
import MagnifyingGlassIcon from "./icons/MagnifyingGlass.astro";
import BarsIcon from "./icons/Bars.astro";
import frustrationLogo from "./images/logo-frustration.webp";
---

<header>
  <nav class="nav">
    <a
      href="/"
      class="logo-frustration"
    >
      <Image
        src={frustrationLogo}
        width="400"
        alt="Logo de Frustration"
      />
    </a>
    <MagnifyingGlassIcon />
    <BarsIcon />
  </nav>
</header>

<style>
  .nav {
    display: flex;
    gap: 20px;
    align-items: center;
    background: var(--secondary-color);
    color: var(--primary-color);
    padding: 10px 30px;
  }

  .logo-frustration {
    margin-right: auto;
    line-height: 0;
  }

  a.logo-frustration img {
    max-width: 300px;
    height: auto;
  }

  svg {
    fill: var(--primary-color);
    width: 25px;
  }

  svg:hover {
    cursor: pointer;
  }
</style>
